+++ new_src/app/src/main/java/com/masterresponde/app/ActiveChatReplyReceiver.java	2026-08-22 17:07:07.000000000 +0000
@@ -0,0 +1,57 @@
+package com.masterresponde.app;
+
+import android.app.RemoteInput;
+import android.content.BroadcastReceiver;
+import android.content.Context;
+import android.content.Intent;
+import android.os.Bundle;
+
+public class ActiveChatReplyReceiver extends BroadcastReceiver {
+
+    public static final String ACTION_REPLY =
+            "com.masterresponde.app.ACTION_ACTIVE_CHAT_REPLY";
+
+    public static final String EXTRA_CONVERSATION =
+            "conversation";
+
+    public static final String EXTRA_GROUP =
+            "group";
+
+    public static final String REMOTE_INPUT_KEY =
+            "master_responde_active_chat_reply";
+
+    @Override
+    public void onReceive(Context context, Intent intent) {
+        if (intent == null
+                || !ACTION_REPLY.equals(intent.getAction())) {
+            return;
+        }
+
+        Bundle results =
+                RemoteInput.getResultsFromIntent(intent);
+
+        if (results == null) {
+            return;
+        }
+
+        CharSequence value =
+                results.getCharSequence(
+                        REMOTE_INPUT_KEY
+                );
+
+        if (value == null
+                || value.toString().trim().isEmpty()) {
+            return;
+        }
+
+        String conversation =
+                intent.getStringExtra(
+                        EXTRA_CONVERSATION
+                );
+
+        WhatsAppAccessibilityService.sendReplyToOpenConversation(
+                conversation == null ? "" : conversation,
+                value.toString()
+        );
+    }
+}
diff -ruN orig_src/app/src/main/java/com/masterresponde/app/MainActivity.java new_src/app/src/main/java/com/masterresponde/app/MainActivity.java
--- orig_src/app/src/main/java/com/masterresponde/app/MainActivity.java	2026-08-18 04:12:28.000000000 +0000
+++ new_src/app/src/main/java/com/masterresponde/app/MainActivity.java	2026-08-22 17:08:52.000000000 +0000
@@ -29,6 +29,7 @@
     private TextView waStatus;
     private TextView masterflixStatus;
     private TextView engineStatus;
+    private TextView activeChatStatus;
 
     private Switch botSwitch;
     private String appliedTheme = "";
@@ -730,7 +731,7 @@
 
         heroText.addView(
                 text(
-                        "Dashboard v1.17.12",
+                        "Dashboard v2.0.3",
                         14,
                         Color.rgb(
                                 255,
@@ -884,6 +885,19 @@
                         this::openNotificationAccess
                 );
 
+        activeChatStatus =
+                addStatus(
+                        statusGrid,
+                        "Chat aberto",
+                        "● Verificando",
+                        Color.rgb(
+                                255,
+                                145,
+                                0
+                        ),
+                        this::openAccessibilityAccess
+                );
+
         content.addView(
                 statusGrid
         );
@@ -907,6 +921,24 @@
         );
 
         gap(
+                10
+        );
+
+        Button accessibilityButton =
+                fullButton(
+                        "ATIVAR RESPOSTA COM CHAT ABERTO",
+                        false
+                );
+
+        accessibilityButton.setOnClickListener(
+                v -> openAccessibilityAccess()
+        );
+
+        content.addView(
+                accessibilityButton
+        );
+
+        gap(
                 12
         );
 
@@ -1243,6 +1275,57 @@
             }
         }
 
+        if (activeChatStatus != null) {
+            boolean enabled =
+                    WhatsAppAccessibilityService.isEnabled(
+                            this
+                    );
+
+            boolean connected =
+                    prefs.getBoolean(
+                            "accessibility_engine_connected",
+                            false
+                    );
+
+            if (enabled && connected) {
+                activeChatStatus.setText(
+                        "● Ativo"
+                );
+
+                activeChatStatus.setTextColor(
+                        Color.rgb(
+                                0,
+                                184,
+                                255
+                        )
+                );
+            } else if (enabled) {
+                activeChatStatus.setText(
+                        "● Ativando"
+                );
+
+                activeChatStatus.setTextColor(
+                        Color.rgb(
+                                255,
+                                145,
+                                0
+                        )
+                );
+            } else {
+                activeChatStatus.setText(
+                        "● Permissão necessária"
+                );
+
+                activeChatStatus.setTextColor(
+                        Color.rgb(
+                                255,
+                                145,
+                                0
+                        )
+                );
+            }
+        }
+
         if (masterflixStatus != null) {
             boolean active =
                     prefs.getBoolean(
@@ -1321,6 +1404,32 @@
         }
     }
 
+    private void openAccessibilityAccess() {
+        try {
+            Intent intent =
+                    new Intent(
+                            android.provider.Settings.ACTION_ACCESSIBILITY_SETTINGS
+                    );
+
+            startActivity(
+                    intent
+            );
+
+            Toast.makeText(
+                    this,
+                    "Ative MASTER RESPONDE — Chat aberto.",
+                    Toast.LENGTH_LONG
+            ).show();
+
+        } catch (Exception e) {
+            Toast.makeText(
+                    this,
+                    "Abra Configurações > Acessibilidade e ative MASTER RESPONDE — Chat aberto.",
+                    Toast.LENGTH_LONG
+            ).show();
+        }
+    }
+
     private void openNotificationAccess() {
         try {
             Intent intent =
diff -ruN orig_src/app/src/main/java/com/masterresponde/app/NotificationAccessService.java new_src/app/src/main/java/com/masterresponde/app/NotificationAccessService.java
